# Export inbox

실 만화 export 폴더를 여기 아래에 두세요.

```text
exports/inbox/my-story/
  meta.json
  pages/
  characters/    (권장)
```

그다음:

```bash
node scripts/run-pipeline.mjs --export exports/inbox/my-story
```

규약: `docs/export-convention.md`  
최종 목표: `docs/GOAL.md`
