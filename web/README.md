# manga-gong-bang-video web (G9)

Next.js surface for clip-package pipeline + Kling job handoff.

See parent docs:

- `docs/GOAL.md` (G9 extension)
- `docs/guides/deployment.md`

```bash
# from repo root
npm run web:dev
# or
cd web && npm install && npm run dev
```

Requires monorepo `scripts/run-pipeline.mjs` (parent directory).
